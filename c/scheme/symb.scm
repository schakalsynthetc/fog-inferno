(define ≤ <=)
(define ≥ >=)
(define C≡ char=?)
(define C< char<?)
(define C≤ char<=?)
(define C> char>?)
(define C≥ char>=?)
(define S≡ string=?)
(define S< string<?)
(define S≤ string<=?)
(define S> string>?)
(define S≥ string>=?)
(define SΣ string-append)
(define √ sqrt)
(define ¬ not)
(define ∧ and)
(define ⋀ and)
(define ∨ or)
(define ⋁ or)
(define ∑ +)
(define ∏ *)
(define ÷ /)
(define · *)
(define × *)
;(define ≡ eqv?)
(define ∅ null?)
(define ℤ integer?)
(define ℚ rational?)
(define ℝ real?)
(define ℂ complex?)
(define ⇐ <-=)
(define ⇒ =<-)
;(define ⊨ #t)
(define ¶ define)
(define ≔ define)
(≔ λ lambda)
(≔ ≠ (λ (x y) (¬ (= x y))))
